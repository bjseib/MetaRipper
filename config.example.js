// Copy this file to config.js (which is git-ignored) and paste the Graph API access token below.
// This keeps sensitive credentials out of version control while still letting the UI read the token.
window.graphConfig = {
  graphApiAccessToken: "YOUR_GRAPH_API_ACCESS_TOKEN_HERE",
};
